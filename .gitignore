# Dependencies
node_modules/
functions/node_modules/

# Build output
dist/
functions/lib/
*.tsbuildinfo

# Environment & secrets
.env*
functions/src/serviceAccountKey.json
firebase.json
firestore.indexes.json

# Local tooling / caches
.firebase/
.vercel/
scripts/.vercel/
*.log
*.cache
*.map

# Private class data (PII — names/emails). Only the two name lists the app
# imports are committed; everything else under src/data stays local.
src/data/*
!src/data/names.ts
!src/data/names-undergrad.ts
scripts/data/

# Tooling output
repomix.config.json
repomix-output.txt

# Editor / OS
.DS_Store

# Agent instructions
CLAUDE.md
